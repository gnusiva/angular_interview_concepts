import { ShowAbove5Pipe } from './show-above5.pipe';

describe('ShowAbove5Pipe', () => {
  it('create an instance', () => {
    const pipe = new ShowAbove5Pipe();
    expect(pipe).toBeTruthy();
  });
});
